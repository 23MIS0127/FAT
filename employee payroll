package com.example;

class Employee {
    int id;
    String name;
    double basicSalary;

    Employee(int id, String name, double basicSalary) {
        this.id = id;
        this.name = name;
        this.basicSalary = basicSalary;
    }

    public double calculateHRA() {
        return basicSalary * 0.20;
    }

    public double calculateDA() {
        return basicSalary * 0.10;
    }

    public double calculateGrossSalary() {
        return basicSalary + calculateHRA() + calculateDA();
    }
}

public class App {
    public static void main(String[] args) {
        Employee emp = new Employee(1, "Vaishnavi", 50000);

        System.out.println("HRA: " + emp.calculateHRA());
        System.out.println("DA: " + emp.calculateDA());
        System.out.println("Gross Salary: " + emp.calculateGrossSalary());
    }
}



package com.example;

import org.junit.Test;
import static org.junit.Assert.*;

public class AppTest {

    @Test
    public void testSalaryCalculation() {
        Employee emp = new Employee(1, "Test", 10000);

        assertEquals(2000, emp.calculateHRA(), 0.01);
        assertEquals(1000, emp.calculateDA(), 0.01);
        assertEquals(13000, emp.calculateGrossSalary(), 0.01);
    }
}
